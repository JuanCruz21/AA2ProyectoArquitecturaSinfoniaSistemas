# Plantilla de configuracion del frontend.
# Copiela como `.env` si necesita valores distintos a los de por defecto:
#   cp .env.example .env

# URL donde escucha la API del backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
